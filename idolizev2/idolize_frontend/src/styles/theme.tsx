import { createTheme } from '@mui/material/styles';

// Create a custom theme
const theme = createTheme({
  palette: {
    primary: {
      main: '#FFB6C1',  // your primary color
    },
    secondary: {
      main: '#FF69B4',  // your secondary color
    },
  },
});

export default theme